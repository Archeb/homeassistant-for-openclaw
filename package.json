{
    "name": "homeassistant-for-openclaw",
    "version": "0.3.3",
    "description": "Home Assistant integration plugin for OpenClaw — sensor context, device control, and logbook access.",
    "type": "module",
    "main": "src/index.ts",
    "license": "Apache-2.0",
    "files": [
        "src/**/*.ts",
        "!src/**/*.test.ts",
        "skills/",
        "openclaw.plugin.json",
        "README.md"
    ],
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "typecheck": "tsc --noEmit",
        "prepublishOnly": "npm run typecheck"
    },
    "peerDependencies": {
        "openclaw": "*"
    },
    "devDependencies": {
        "openclaw": "*",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    },
    "openclaw": {
        "extensions": [
            "./src/index.ts"
        ]
    }
}